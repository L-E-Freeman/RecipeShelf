function addMore(selector, type) {
    /* Selector parameter is being duplicated. This parameter
    should be the element you want to copy - e.g, ingredients field. */
    var newElement = $(selector).clone(true);
    


}